//move direction
public enum Direction {
	NONE, LEFT, RIGHT
}

//move state
public enum MoveState {
	STAY, WALK, JUMP
}

//animation type
public enum AnimationType {
	NONE, STAY, WALK, JUMP, DAMAGED, DIE
}